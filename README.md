# Scholub

Scholub monorepo — frontend, backend, crawler, and LLM server.

## Structure

- `frontend/` — web client
- `backend/` — API server
- `crawler/` — arXiv crawler / paper reviewer service
- `llm-server/` — LLM inference / search server
